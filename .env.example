# LiveKit Cloud
LIVEKIT_URL=wss://your-livekit-project.livekit.cloud
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# xAI Grok — single key powers STT + LLM + TTS in the cascaded pipeline.
# Get a key from https://console.x.ai/
XAI_API_KEY=your_xai_api_key

# --- Cascaded Grok pipeline knobs (all optional) ---
# LLM model. grok-4-fast-reasoning gives chain-of-thought + good TTFT and
# is the most reliable at calling log_verification correctly. Fallbacks:
#   - "grok-4" (slower, more accurate)
#   - "grok-3-mini-fast" (cheaper; pair with XAI_REASONING_EFFORT=high)
XAI_LLM_MODEL=grok-4-fast-reasoning
# Reasoning depth: low | medium | high. For routing decisions in this
# verification flow, "low" is sufficient and keeps TTFT down.
XAI_REASONING_EFFORT=low
# Lower temperature = more deterministic tool calling. 0.3 is the sweet spot.
XAI_LLM_TEMPERATURE=0.3
# TTS voice (xai.TTS uses lowercase voice names):
#   ara (female, default), eve (female), leo (male), rex (male), sal (male)
GROK_VOICE=ara

# --- Latency knobs (all optional, sane defaults below) ---
# AEC warmup blocks the agent from speaking for this many seconds after
# session.start. Default in livekit-agents is 3.0s. SIP audio is
# unidirectional through TCN's gateway so AEC isn't relevant — keep at 0.
AEC_WARMUP_DURATION=0
# Let the LLM start composing replies before the user fully ends a turn.
# Big mid-call latency win. Set to "false" only if you see prematurely-cut
# generations.
PREEMPTIVE_GENERATION=true
# How long after the user stops speaking before we declare turn-end.
# Lower = snappier, higher = fewer false interruptions.
MIN_ENDPOINTING_DELAY=0.4
MAX_ENDPOINTING_DELAY=3.0

# Existing Railway webhook server (Node app — unchanged)
RAILWAY_SERVER_URL=https://your-existing-railway-webhook.up.railway.app

# Core LiveKit Agents — `~=1.4` allows any 1.x >= 1.4 (including 1.5.x).
# The [xai] extra brings in livekit-plugins-xai automatically, which provides
# xai.realtime.RealtimeModel for the Grok Realtime voice agent.
livekit-agents[xai]~=1.4

# Silero VAD — required for the cascaded pipeline's turn detection.
# Loaded once per worker process via prewarm_fnc.
livekit-plugins-silero~=1.4

# Direct LiveKit API (room/participant management, sip dispatch helpers)
livekit-api>=1.0

python-dotenv>=1.0.0
aiohttp>=3.9.0

# --- Observability (Prometheus + Grafana) ---
# prometheus-client: metric types + the /metrics scrape endpoint.
prometheus-client>=0.20
# prometheus-remote-writer: pure-Python (no snappy/C deps) Prometheus
# remote-write client. Used only when METRICS_MODE includes "remote_write"
# to push metrics from the managed (scale-to-zero) Cloud agent to Grafana Cloud.
prometheus-remote-writer>=1.1

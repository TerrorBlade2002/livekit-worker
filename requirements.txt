# Core LiveKit Agents — needs >=1.5 for Adaptive Interruption Handling,
# LiveKit Inference (STT/LLM/TTS), inference.TurnDetector, and the cascaded
# TurnHandlingOptions (preemptive generation, interruption modes).
#
# The default pipeline runs through LiveKit Inference (no provider keys needed
# beyond LIVEKIT_*). The extras below pull in the direct provider plugins that
# back the `*_BACKEND=plugin` escape hatch and the AssemblyAI A/B STT path.
livekit-agents[deepgram,openai,cartesia,assemblyai,silero,turn-detector]~=1.5

# Silero VAD — loaded once per worker process via the prewarm (server.setup_fnc).
livekit-plugins-silero~=1.5

# Krisp background voice cancellation (BVCTelephony) for SIP/telephony input.
# Optional: set NOISE_CANCELLATION=off if not deployed to LiveKit Cloud.
# NOTE: this plugin is on its own 0.2.x version line (not 1.x).
livekit-plugins-noise-cancellation>=0.2

# Direct LiveKit API (room/participant management, SIP dispatch helpers)
livekit-api>=1.0

python-dotenv>=1.0.0
aiohttp>=3.9.0

# --- Observability (Prometheus + Grafana) ---
# prometheus-client: metric types + the /metrics scrape endpoint.
prometheus-client>=0.20
# prometheus-remote-writer: pure-Python (no snappy/C deps) Prometheus
# remote-write client. Used only when METRICS_MODE includes "remote_write"
# to push metrics from the managed (scale-to-zero) Cloud agent to Grafana Cloud.
prometheus-remote-writer>=1.1
